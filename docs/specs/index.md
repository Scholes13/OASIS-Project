# Specs Index

Use this directory for long-lived product and feature specifications that should remain easy for both humans and agents to discover.

## Current State

- New stable specs should prefer this directory.
- Existing migrated spec artifacts from the older `docs/superpowers/specs/` area now live here.
- `docs/superpowers/specs/` remains only as a navigation shim for the old path.

## Notes

- Keep specs focused on product behavior and acceptance criteria.
- Avoid duplicating implementation plans here.
- Keep filenames stable when migrating specs so historical references stay easy to trace.
