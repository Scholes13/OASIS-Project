# Superpowers Docs Index

This directory is now a navigation shim for the old `docs/superpowers/` paths used before the documentation cleanup.

## Current Mapping

- `docs/superpowers/specs/` -> migrated to `docs/specs/`
- `docs/superpowers/plans/` -> migrated to `docs/plans/`

## Guidance

- Prefer `docs/specs/` for specs.
- Prefer `docs/plans/` for design notes and implementation plans.
- Keep this directory only for index pages that explain the migration.
