# Superpowers Specs Index

This directory no longer stores dated spec artifacts. Those files were migrated to `docs/specs/`.

## Current Role

- Treat this directory as a pointer to the new canonical spec location.
- Historical references should be updated to `docs/specs/` when touched.

## Preferred Future Location

- New and migrated specs live in `docs/specs/`.
